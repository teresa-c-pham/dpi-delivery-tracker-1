<!DOCTYPE html>
<html>
  <head>
    <title>Delivery Tracker 1</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if (current_user == nil) %>
      <a href="/users/sign_up">Sign up</a> | <a href="/users/sign_in">Sign in</a>
      <hr>
      <p>You need to sign in or sign up before continuing.</p>
    <% else %>
      <a href="/deliveries">Delivery Tracker</a> | <a href="/users/edit">Edit profile</a> | <a href="/users/sign_out">Sign out</a>
    <% end %>

    <%= yield %>
  </body>
</html>
